# auto-document 项目定位与功能定义草案

## 文档状态

这是一份用于校准本项目自身定位的说明文档草案。
当前目标不是冻结最终表述，而是先把项目的目标、工作状态、能力边界和使用前提讲清楚，作为后续调整 `SKILL.md`、脚本流程和状态机的依据。

## 定位

`auto-document` 是一个面向 Codex 或类似 AI 协作场景的仓库文档工作流 skill。
它不是一个“扫描代码后自动产出说明”的通用文档工具，而是一个在人类校准项目意图之后，按功能树持续维护项目文档的协作系统。

## 一句话定义

这个 skill 用于在主分支或即将合并到主分支的仓库环境中，由人先校准项目当前状态与未来方向，再让 AI 按功能树而不是目录树生成和维护项目文档。

## 项目定位

这个项目不是单纯的“自动生成文档脚本集合”，而是一个带明确工作流约束的文档协作系统。
它的核心价值不在于一次性产出大量文档，而在于先为 AI 建立可靠的项目认知基线，再让后续文档生成和维护围绕这条基线进行。

从角色上看，这个项目首先服务的是人类维护者。
AI 在这里承担的是协作与维护责任，而不是文档真相的最终定义者。
因此，这个项目的输出始终面向人类阅读和长期维护，不再区分“给 AI 的文档”和“给人类的文档”两套系统。

从职责上看，这个项目更接近“文档工作流约束器”，而不是“文档排版器”或“通用知识库生成器”。
它试图解决的核心风险包括：

- AI 把临时实现误写成长期稳定架构
- 文档只对齐当前代码，却没有对齐项目真正想去的方向
- 模块说明按目录展开，导致目录结构被误当成功能边界
- 代码变化后只做局部补丁，却没有判断何时应该整体收敛
- 项目在大范围变动后，旧文档继续被保留，最终变得不清晰甚至误导

## 适用环境

这个 skill 不是面向所有仓库环境都默认启用的。
当前更明确的适用前提是：

- 目标仓库必须处于可用的 git 环境中
- 主要面向主分支，或即将合并到主分支上的环境
- 主要面向单线维护或低并发协作场景
- 由用户明确要求使用时才触发，而不是持续自动运行

这意味着它当前不适合以下场景：

- 没有 git 的仓库
- 多人同时对同一文档系统并发维护的场景
- 尚未明确要进入主分支或稳定交付链的临时实验分支
- 希望后台自动巡检、自动改写文档的持续运行场景

## 核心问题

这个项目试图解决的不是“仓库里没有文档”这么简单的问题，而是下面几个更具体的问题：

- AI 在第一次接手项目时，容易把目录结构误当成功能边界
- 项目当前实现和长期目标之间经常存在偏差，但文档里没有被明确区分
- 模块级文档如果没有项目级意图基线，容易把阶段性结构固化为稳定设计
- 代码变更后，文档往往只会局部补丁，缺少判断何时增量更新、何时整体收敛的机制
- 在项目发生大量变动后，旧的功能边界、代码映射和文档描述常常继续并存，最终让文档失去可信度

因此，这个项目的目标不是生成“更多文档”，而是生成“更可信、可维护、能持续与项目演进对齐的文档”。

## 核心理念

- 代码是当前实现真相
- 用户调整后的 `project-summary.md` 是项目意图基线
- AI 可以协助判断用户是否已经完成确认，但不能跳过人工校准这个过程
- 功能树是真正的文档主骨架，代码树只是功能树在代码中的映射
- 文档应按功能拆解，而不是按目录自动拆解
- git 只负责判断文档与代码是否对齐，不负责替代人类确认项目方向
- 如果代码变化没有改变文档语义，就不应为了“代码变了”而强行重写文档

这些理念共同决定了本项目的默认工作方式：

- 先确认项目现在是什么、未来准备往哪里走，再继续描述功能边界
- 先建立功能树和代码映射关系，再继续展开下级模块说明
- 当变化已经超出局部补丁范围时，优先收敛而不是继续累积局部修补
- 当变化不影响原有描述时，只更新 git 对齐点，不轻易改写文档正文

## 目标用户

### 直接使用者

- 需要为仓库建立长期可维护项目文档的开发者
- 正在带着 AI 一起维护仓库的个人或团队
- 希望在主分支交付链上持续保持文档与实现一致的维护者

### 间接受益者

- 后续接手该仓库的新成员
- 需要快速理解仓库功能边界与代码入口的 AI agent
- 需要在重构、排障、增量开发中快速获得项目上下文的协作者

## 主要产物

项目当前希望围绕以下几类文档产物组织能力：

- `overview/project-summary.md`
  项目级意图基线，用于回答“这个项目现在是什么、未来希望往哪里演进、当前哪些设计已经稳定”
- `overview/project-structure.md`
  功能树到代码树的映射工具，用于回答“某一层功能在代码中落在哪些路径和文件”
- `modules/`
  功能域文档集合，用于回答“这个功能域解决什么问题、与上下级功能是什么关系、关键入口在哪里”
- `index.json`
  机器可读控制面，用于记录阶段状态、功能域索引、git 对齐点和待更新项
- `history/`
  用于记录分析轨迹、维护过程和变更规划

这里的默认文件和目录是工作流占位，不是不可变化的最终结构。
它们的职责应该稳定，但具体内容和下钻方式可以随着功能树和项目状态继续调整。

## 核心能力定义

### 1. 初始化文档系统

为目标仓库建立默认文档根目录、索引文件和基础骨架，并记录当前 git 对齐点。
如果目标仓库没有可用 git 环境，则当前不进入这套 skill 工作流。

### 2. 生成项目级 summary 草案

在第一次正式分析模块之前，先基于 README、关键入口和代码结构生成项目级理解草案。
这份草案必须同时覆盖：

- AI 对项目当前状态的理解
- 需要用户补充和修正的未来方向、设计理念和架构意图

### 3. 确认项目级意图基线

只有当用户已经对 summary 做出足够校准后，后续结构分析和模块生成才有资格继续。
这里的确认不要求必须是严格的显式命令确认，它可以表现为：

- 用户直接修改了 `project-summary.md`
- 用户明确表示这份 summary 可以作为后续基线
- 用户要求继续进入下一阶段，且上下文已足以让 AI 判断 summary 已被接受

换句话说，这里是“人工校准 + AI 协助判断”的确认语义，而不是形式化审批流。

### 4. 建立功能树与代码树映射

在 summary 已确认后，识别项目的主要功能域，并按功能而不是按目录进行树级拆解。
这棵树会逐层向下拆，直到落到已经不需要继续依赖下级功能来解释的最小功能单元。

在这一步中，还需要建立对应的代码树映射关系：

- 功能树描述功能层级
- 代码树描述这些功能分别落在哪些路径和文件
- 代码树的最小粒度是文件，而不是功能最小单元

因此，`project-structure.md` 的核心职责不再是单纯解释技术栈目录语义，而是作为“功能树到代码树”的映射工具，服务后续代码阅读与功能定位。

### 5. 生成功能域文档

在 summary 和功能树都已建立后，围绕功能域、职责边界、上下级关系、关键入口和阅读顺序生成模块文档。
模块文档的最小组织单位应优先按功能域定义，而不是按路径或单文件定义。

模块文档重点回答：

- 这个功能解决什么问题
- 它在功能树中的位置是什么
- 它依赖哪些下级功能，服务哪些上级功能
- 它在代码中落在哪些路径和文件
- 继续阅读时应该先看哪里

### 6. 基于 git 的增量维护

在用户要求维护文档时，对比当前代码与上次对齐的 git 状态，找出发生变化的文件，再通过功能树和搜索工具定位最小受影响功能模块。

维护的基本原则是：

- 先改最小受影响功能模块
- 如果影响已经扩散到多个兄弟节点，则向上层功能域收敛
- 如果只是文件变了，但原文档描述仍然成立，则不改正文，只更新 git 对齐点
- 如果新增或删除了文件，则功能树、代码树和相关文档都应同步调整

### 7. 大范围变化后的收敛

当项目发生重构、路径迁移、功能域重划或定位变化时，不继续依赖局部补丁，而是重新评估 summary、功能树和模块文档的有效性，并将文档系统重新对齐到当前状态。

如果变化已经触及项目根本定位、核心设计理念或主要功能边界，则应要求用户复核，而不是由 AI 直接固化新的解释。

### 8. 人工驱动的功能分支调整

用户可以要求删除某个功能分支、增加某个功能分支，或者保留一部分暂时与代码树不一致的功能文档。
这时系统应先做两件事：

- 确认该功能分支在功能层级中的位置是否合理
- 理解它与现有代码树、功能树之间的关系

如果用户维护的功能分支与当前代码树不一致，不应直接覆盖用户意图，而应先询问该差异是否代表有效设计，再决定如何更新映射和文档。

## 工作状态定义

当前这个项目更适合被理解为一个“人工触发、按阶段推进、按功能维护”的工作流系统。
它的工作状态大致可以收敛成以下几个阶段：

1. 初始化完成
   已建立占位骨架和 git 基线，但尚未形成可信项目理解。
2. summary 草案阶段
   AI 给出项目当前状态理解，等待用户补充、修正和校准未来方向。
3. summary 已确认
   AI 可以把用户调整后的 summary 作为后续功能树分析基线。
4. 功能树与代码树建立阶段
   开始把项目拆解成功能层级，并建立对应代码映射。
5. 功能域文档生成阶段
   按功能树从上到下生成功能文档。
6. 维护阶段
   用户再次触发 skill 时，根据 git 变化和人工要求更新最小受影响功能模块。
7. 收敛阶段
   当变化已超出局部修补范围时，重新评估并重建部分或全部文档系统。

## 差异化价值

相较于常见的自动文档工具，这个项目的差异化价值主要体现在：

- 强制把“人类校准后的项目意图”放在工作流前面
- 明确用功能树而不是目录树作为文档主骨架
- 把功能树和代码树区分开来处理
- 用 git 感知来判断文档是否落后，但不把 git 当成项目意图的来源
- 在增量维护和整体收敛之间提供明确切换机制
- 将“帮助 AI 在长期协作中持续正确理解项目”作为核心目标，而不只是生成一次性说明文本

## 明确的非目标

为了避免项目边界不断扩散，建议明确以下内容不属于当前主目标：

- 不是通用博客或静态站点生成器
- 不是单纯依赖目录树自动总结代码的摘要器
- 不是完全无人参与的一键文档工厂
- 不是替代架构设计本身的工具
- 不是以视觉展示或发布页面为优先目标的内容管理系统
- 不是持续后台自动监控并自动改写文档的守护进程
- 不是优先为本仓库自身变化提供高度敏感自维护能力的项目

## 成功标准

如果这个项目定位成立，那么后续设计应尽量满足以下标准：

- AI 在第一次接手仓库时，能快速区分项目当前实现和长期方向
- 文档不会轻易把临时结构误写成稳定边界
- 模块文档的拆分依据是功能层级，而不是目录层级
- 维护者能清楚知道当前应更新 summary、功能树还是具体功能模块
- 代码变化后，系统能判断应该局部更新、向上收敛，还是仅更新 git 基线
- 后续 agent 能把这套文档当成可信上下文继续工作，而不是把它视为噪音或过期说明

## 当前建议的产品边界

现阶段更适合把本项目定义为：

一个面向 Codex 或类似 AI 协作场景、主要服务主分支环境的仓库文档工作流 skill。

这意味着它当前优先追求的是：

- 工作流正确性
- 功能树拆解质量
- 文档与代码映射关系的清晰度
- 状态流转可解释性
- 与真实代码和 git 状态的一致性

而不是优先追求：

- UI 展示
- 发布渠道
- 花哨的内容格式
- 与大量外部平台的集成

## 已明确的决策

基于当前讨论，下面这些问题已经有了明确方向：

- 项目的核心对象是 Codex skill，而不是通用 Python 工具链
- `project-summary.md` 的确认语义是人工校准后由 AI 协助判断，而不是严格审批流
- 模块文档的最小组织粒度优先按功能域，而不是按路径或单文件
- 维护模式由人工触发，而不是自动巡检触发
- 文档系统始终面向人类阅读，不区分 AI 专用文档层

## 推荐的后续动作

如果这份定位方向成立，后续工作可以优先按这个顺序推进：

1. 用这份草案修订 `SKILL.md`，统一描述语言、阶段定义和适用环境
2. 对照正式定位梳理脚本状态机与 `index.json` 字段职责
3. 把“功能树优先、人工触发、git 前提、模糊确认”的语义落实到脚本实现
4. 再修正文档生成、维护规划和收敛流程中的行为偏差

